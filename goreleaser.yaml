version: 2

project_name: gtrace
 
before:
  hooks:
    - go mod tidy
    # Build the frontend assets so they are ready for embedding
    - sh -c "cd frontend && npm run build"
 
builds:
  - id: gtrace-macos
    env:
      - CGO_ENABLED=1
    goos:
      - darwin
    goarch:
      - arm64
    flags:
      - -tags=desktop,production
    ldflags:
      - -s -w -extldflags "-framework UniformTypeIdentifiers"

archives:
  - id: gtrace-default
    format: tar.gz
    # This wraps the binary in a tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- .Os }}_
      {{- .Arch }}
    builds:
      - gtrace-macos

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
